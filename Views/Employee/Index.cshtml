@model IEnumerable<Employeewithef.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div >
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })

    @using (Html.BeginForm("Index", "Employee", FormMethod.Get, new { style = "margin-top: 10px;" }))
    {

        @Html.TextBox("searching")
        <input type="submit" value="Search" />


    }

    <table class="table table-striped">
    </table>

</div>

<table class="table table-hover table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DOJ)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Designation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salary)
        </th>
        <th>
   
        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="5" style="color:red">
                    No match any document

                </td>
            </tr>
        }

    </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DOJ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Designation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>




            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.empid }, new { @class = "btn btn-primary" }) |
                @Html.ActionLink("Details", "Details", new { id = item.empid }, new { @class = "btn btn-warning" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.empid }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
